// Un solo lugar para apuntar el frontend a tu backend.
// En local: déjalo así. Cuando despliegues el backend (Render/Railway),
// reemplaza esta URL por la que te den, ej: 'https://tu-servicio.onrender.com'
const CONFIG = {
  API_BASE_URL: 'http://localhost:4000',
};
