PORT=4000
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio

# OBLIGATORIA desde agosto 2026: dirección de conexión a tu clúster de
# MongoDB Atlas (gratis) — sin esto, el servidor no arranca. La consigues
# creando una cuenta gratuita en https://www.mongodb.com/cloud/atlas y
# copiando el "Connection string" de tu clúster (botón "Connect" ->
# "Drivers"). Se ve algo así:
# mongodb+srv://usuario:contraseña@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=

# Opcional: nombre de la base de datos dentro de tu clúster de Mongo donde
# se guarda todo. Si lo dejas vacío, se usa "zenith_capital" por defecto —
# no hace falta crearla a mano, Mongo la crea sola la primera vez.
MONGODB_DB_NAME=

# Necesaria para la Asesoría IA avanzada y la auto-inversión (solo
# insignias Diamante/Platino, ver routes/ai.js). Consíguela en
# https://platform.openai.com/api-keys
# Sin esto, esas dos funciones responden 503 (no rompe el resto de la app).
OPENAI_API_KEY=

# Opcional: verifica en tu dashboard de OpenAI cuál es el modelo económico
# vigente antes de asumir que este sigue existiendo (los nombres cambian).
OPENAI_MODEL=gpt-5.6-luna

# Opcional: para mostrar el precio real de Petróleo (WTI/Brent) y de los
# índices bursátiles (S&P 500, Dow Jones, Nasdaq 100) en el panel de
# trading (ver data/markets.js). Gratis, sin tarjeta — consíguela en
# https://www.alphavantage.co/support/#api-key (solo pide correo). Su plan
# gratis es muy limitado (25 peticiones/día en total), por eso el precio
# se guarda en caché varias horas — de todos modos ese plan solo da el
# cierre del día anterior, no el precio exacto en vivo del segundo. Sin
# esta variable, esos activos aparecen como "no disponible" en vez de
# romper el resto del panel.
ALPHA_VANTAGE_API_KEY=

# Opcional: si defines esto, TODO el sitio (incluida la pantalla de login)
# queda bloqueado detrás de un código de acceso único hasta que se
# escriba correctamente — pensado para que nadie que llegue al link por
# accidente pueda ni ver el login. Si lo dejas vacío, el sitio queda
# público como hasta ahora.
SITE_ACCESS_CODE=

# Opcional: código separado del anterior, solo para el panel de
# administrador (admin.html), donde se aprueban/editan/rechazan los
# depósitos, retiros, compras y ventas pendientes. Si lo dejas vacío, ese
# panel no funciona (no expone nada por error).
ADMIN_CODE=
